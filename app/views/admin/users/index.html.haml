%h1 Listing users

%table
  %tr
    %th Username
    %th Password salt
    %th Password hash
    %th Persistence token
    %th Perishable token
    %th Single access token
    %th Last request at
    %th
    %th
    %th

  - @users.each do |user|
    %tr
      %td= user.username
      %td= current_user.username if current_user
      %td= user.password_hash
      %td= user.persistence_token
      %td= user.perishable_token
      %td= user.single_access_token
      %td= user.last_request_at
      %td= link_to 'Show',  admin_user_path(user)
      %td= link_to 'Edit', edit_admin_user_path(user)
      %td= link_to 'Destroy', admin_user_path(user), :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New User', new_admin_user_path
